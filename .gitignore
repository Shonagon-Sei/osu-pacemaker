node_modules/
.cache/
.env
*.log

# Build output + baked config (may contain deploy URLs/keys)
dist/
config/runtime.json

# Generated icons (recreated by `npm run gen-icon`)
build/icon.png
build/tray.png

# Sample replays for dev/verification (not shipped)
samples/

# Editor / tooling
.claude/
.vscode/
.idea/
.DS_Store
Thumbs.db
